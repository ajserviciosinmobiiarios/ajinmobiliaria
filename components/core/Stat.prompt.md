Large serif credential figure with an uppercase label beneath.

```jsx
<Stat value="+25M€" label="Patrimonio gestionado" />
<Stat value="Galardonado" label="Premios Century 21" tone="onDark" />
```

Tones: `ink` (light bg), `onDark` (figure becomes gold). Use for hero trust strip.
