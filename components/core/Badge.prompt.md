Small squared uppercase label for tags, service badges and awards.

```jsx
<Badge>Servicio Personal Shopper</Badge>
<Badge variant="solid">Exclusiva</Badge>
```

Variants: `outline` (default), `solid` (gold), `soft` (gold tint), `dark`. Never rounded.
